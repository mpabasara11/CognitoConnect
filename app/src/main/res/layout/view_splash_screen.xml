<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="@drawable/custom_gradient"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Controllers.Controller_SplashScreen">


    <ImageView
        android:id="@+id/img_logo"
        android:layout_width="293dp"
        android:layout_height="277dp"
        android:layout_gravity="center"
        android:layout_marginTop="158dp"
         app:tint="#A8B0DC"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.491"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/cognito_connect" />

    <ProgressBar
        android:id="@+id/pgb_loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:visibility="visible"
        app:layout_constraintEnd_toEndOf="@+id/img_logo"
        app:layout_constraintHorizontal_bias="0.491"
        app:layout_constraintStart_toStartOf="@+id/img_logo"
        app:layout_constraintTop_toBottomOf="@+id/img_logo" />

    <TextView
        android:id="@+id/lbl_loading_status"
        android:layout_width="195dp"
        android:layout_height="36dp"
        android:layout_marginTop="8dp"
        android:textColor="@color/white"
        android:text="Verifying Internet Connection..."
        android:textAlignment="center"
        app:layout_constraintEnd_toEndOf="@+id/pgb_loading"
        app:layout_constraintStart_toStartOf="@+id/pgb_loading"
        app:layout_constraintTop_toBottomOf="@+id/pgb_loading" />

</androidx.constraintlayout.widget.ConstraintLayout>